package com.sda.fundamentals;

public class DemoAssignment {

    // assignment (=)
    int x = 10;

    // relational (> <)
    // expression
    public void compare() {
        boolean expression = 5 > 2; // ctrl + shift + up / down

        if (5 > 2) {

        }
    }

    // logical (true / false)
    public void doSomething() {
        boolean expression1 = true;
        boolean expression2 = false;

//        expression1 && expression2; // false
//        expression1 || expression2; // false

        if (expression1 && expression2) {
            // return false;
        }
    }
}
